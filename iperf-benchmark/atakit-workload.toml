# Minimal iperf3 server workload for CVM network benchmarking.

format = 5

[workload]
name = "iperf-benchmark"
version = "v0.1.1"
base-image-mode = "blacklist"
image = { build = ".", containerfile = "Containerfile" }
ports = ["5201:5201"]
restart = "unless-stopped"
